import Foundation

// Represents value shown in the middle column (if any) for a portfolio-related view -- plan to combine into another file
enum PortfolioMiddleColumn {
    case holdings, costBasis, profitLoss, none
}
